"""
jalouzee_blinds — an ESPHome external component for controlling the tilt
angle of venetian blind slats.

All functionality is implemented as a `cover` platform, so the actual logic
(CONFIG_SCHEMA / to_code) lives in cover.py. This file is intentionally left
empty — it just makes the directory recognizable as an ESPHome component
Python package.

For architecture details and assumptions, see README.md at the repo root.
"""
